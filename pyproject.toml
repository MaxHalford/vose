[project]
name = "vose"
version = "0.1.0"
description = "Cython implementation of Vose's Alias method"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ['setuptools', 'wheel', 'Cython', 'numpy>=1.22']
